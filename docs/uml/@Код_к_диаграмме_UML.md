```
@startuml
start
:Начало оценки ответа;

if (Проверяем наличие элементов) then (да)
  :1. Определение понятий;
  if (Есть?) then (да)
    :✅ Есть;
  else (нет)
    :❌ Нет;
  endif
  
  :2. Для чего используется;
  if (Есть?) then (да)
    :✅ Есть;
  else (нет)
    :❌ Нет;
  endif
  
  :3. Применение;
  if (Есть?) then (да)
    :✅ Есть;
  else (нет)
    :❌ Нет;
  endif
  
  :4. Диаграммы UML;
  if (Есть?) then (да)
    :✅ Есть;
  else (нет)
    :❌ Нет;
  endif
  
  :5. Инструменты;
  if (Есть?) then (да)
    :✅ Есть;
  else (нет)
    :❌ Нет;
  endif
  
  :Формирование итоговой оценки;
  note right
    Итог: 4 из 5 критериев выполнено
    Оценка 4, но можно было лучше
  endnote
  stop
else (нет)
  :Нет элементов для проверки;
  stop
endif
@enduml
```

### Диаграмма

```

           ┌─────────────────────────────────────┐
           │   Процесс оценки ответа студента    │
           └──────────────────┬──────────────────┘
                              │
                              ▼
                     [Начало оценки ответа]
                              │
                     ┌────────┴────────┐
                     ▼                 ▼
           [Есть элементы для     [Нет элементов
             проверки?]           для проверки]
                     │                 │
               ┌─────┴─────┐           │
               ▼           ▼           ▼
            [ДА]         [НЕТ]   [Завершение:
                     │           нет элементов]
                     ▼
           [Критерий 1: Определение понятий]
                     │
               ┌─────┴─────┐
               ▼           ▼
            [✅ Есть]   [❌ Нет]
               │           │
               └─────┬─────┘
                     ▼
           [Критерий 2: Для чего используется]
                     │
               ┌─────┴─────┐
               ▼           ▼
            [✅ Есть]   [❌ Нет]
               │           │
               └─────┬─────┘
                     ▼
           [Критерий 3: Применение]
                     │
               ┌─────┴─────┐
               ▼           ▼
            [✅ Есть]   [❌ Нет]
               │           │
               └─────┬─────┘
                     ▼
           [Критерий 4: Диаграммы UML]
                     │
               ┌─────┴─────┐
               ▼           ▼
            [✅ Есть]   [❌ Нет]
               │           │
               └─────┬─────┘
                     ▼
           [Критерий 5: Инструменты]
                     │
               ┌─────┴─────┐
               ▼           ▼
            [✅ Есть]   [❌ Нет]
               │           │
               └─────┬─────┘
                     ▼
           [Формирование итоговой оценки]
                     │
                     ▼
           ┌─────────────────────────┐
           │ ИТОГ:                   │
           │ 4 из 5 критериев        │
           │ выполнено               │
           │                         │
           │ Оценка: 4               │
           │ Комментарий: можно было │
           │ лучше                   │
           └─────────────────────────┘

```


































