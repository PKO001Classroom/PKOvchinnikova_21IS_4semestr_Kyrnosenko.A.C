# Техническое задание 
## Аналитический журнал

**Версия документа:** 1.0
**Дата создания:** 21.01.2026  
**Автор:** Курносенко Александр Сергеевич, группа 21ИС-24  
**Статус:** Учебный проект
**Руководитель практики:** Бобошко Михаил Николаевич

---

# 🛠️ ТЕХНИЧЕСКОЕ ЗАДАНИЕ (Technical Specification Document - TSD)
## 1. Архитектура системы
### 1.1. Высокоуровневая архитектура:
```
┌─────────────────────────────────────────────────┐
│                Desktop Application              │
├─────────────────────────────────────────────────┤
│  GUI Layer (Tkinter)     │  Business Logic     │
├──────────────────────────┼─────────────────────┤
│  Database Layer          │  File System Layer  │
│  (PostgreSQL)            │  (Markdown files)   │
└──────────────────────────┴─────────────────────┘
```
### 1.2. Компоненты системы:
- Frontend: Tkinter GUI
- Backend: Python бизнес-логика
- Database: PostgreSQL
- Storage: Локальная файловая система

## 2. Технологический стек
### 2.1. Основные технологии:
- Язык программирования: Python 3.8+
- GUI Framework: Tkinter
- База данных: PostgreSQL 12+
- ORM: psycopg2
- Отчёты: openpyxl, reportlab
- Тестирование: pytest, unittest

### 2.2. Зависимости:
```
core:
  - python >= 3.8
  - tkinter (стандартная библиотека)
  
database:
  - psycopg2-binary >= 2.9.0
  - sqlalchemy >= 1.4.0 (опционально)
  
file_processing:
  - markdown >= 3.0
  - python-frontmatter >= 1.0
  
reporting:
  - openpyxl >= 3.0
  - reportlab >= 3.6
  - matplotlib >= 3.5 (для графиков)
  
testing:
  - pytest >= 7.0
  - pytest-cov >= 3.0
  - pytest-qt >= 4.0
```

## 3. Детальные технические требования
### 3.1. База данных:
```
-- Таблица конспектов
CREATE TABLE notes (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    file_path VARCHAR(500) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    category VARCHAR(100),
    word_count INTEGER DEFAULT 0,
    is_archived BOOLEAN DEFAULT FALSE
);

-- Таблица активности
CREATE TABLE activity_log (
    id SERIAL PRIMARY KEY,
    user_id INTEGER,
    note_id INTEGER REFERENCES notes(id),
    action_type VARCHAR(50),
    action_timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    duration_seconds INTEGER,
    details JSONB
);

-- Индексы для оптимизации
CREATE INDEX idx_notes_category ON notes(category);
CREATE INDEX idx_notes_created ON notes(created_at);
CREATE INDEX idx_activity_user_date ON activity_log(user_id, action_timestamp);
```

### 3.2. Структура файлов:
```
project/
├── src/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py              # Точка входа
│   │   ├── gui/
│   │   │   ├── __init__.py
│   │   │   ├── main_window.py   # Главное окно
│   │   │   ├── notes_panel.py   # Панель конспектов
│   │   │   └── analytics_panel.py # Панель аналитики
│   │   ├── core/
│   │   │   ├── __init__.py
│   │   │   ├── database.py      # Работа с БД
│   │   │   ├── file_manager.py  # Управление файлами
│   │   │   └── models.py        # Модели данных
│   │   ├── services/
│   │   │   ├── __init__.py
│   │   │   ├── analytics.py     # Аналитика
│   │   │   ├── reporting.py     # Генерация отчётов
│   │   │   └── achievements.py  # Система достижений
│   │   └── utils/
│   │       ├── __init__.py
│   │       ├── validators.py    # Валидаторы
│   │       └── helpers.py       # Вспомогательные функции
├── tests/
│   ├── __init__.py
│   ├── test_database.py
│   ├── test_file_manager.py
│   ├── test_analytics.py
│   └── test_gui.py
├── data/
│   ├── notes_md/               # Markdown файлы
│   ├── exports/                # Экспортированные отчёты
│   └── config/                 # Конфигурационные файлы
├── docs/
│   ├── api/                    # API документация
│   └── user_manual/            # Руководство пользователя
└── requirements.txt
```

## 4. API спецификация (внутренние интерфейсы)
### 4.1. DatabaseManager:
```
class DatabaseManager:
    def __init__(self, connection_string: str):
        self.connection_string = connection_string
    
    def create_note(self, title: str, category: str, content: str) -> int:
        """
        Создание нового конспекта
        
        Args:
            title: Заголовок конспекта
            category: Категория
            content: Содержимое в Markdown
        
        Returns:
            ID созданного конспекта
        """
        pass
    
    def get_note_statistics(self, start_date: datetime, end_date: datetime) -> dict:
        """
        Получение статистики за период
        
        Returns:
            dict: Статистика по категориям, активности и т.д.
        """
```

4.2. ReportGenerator:
```
class ReportGenerator:
    def generate_excel_report(self, data: dict, output_path: str) -> bool:
        """
        Генерация Excel отчёта
        
        Args:
            data: Данные для отчёта
            output_path: Путь для сохранения
        
        Returns:
            bool: Успешность генерации
        """
        pass
    
    def generate_pdf_report(self, data: dict, template_path: str, output_path: str) -> bool:
        """
        Генерация PDF отчёта
        
        Args:
            data: Данные для отчёта
            template_path: Путь к шаблону
            output_path: Путь для сохранения
        
        Returns:
            bool: Успешность генерации
        """
```

## 5. Требования к интерфейсу
### 5.1. Главное окно:
- Минимальный размер: 1024x768
- Поддержка тёмной/светлой темы
- Адаптивная верстка
- Горячие клавиши

### 5.2. Элементы управления:
- TreeView для списка конспектов
- Text widget с подсветкой Markdown
- Диалоговые окна для операций
- Progress bar для длительных операций

## 6. Требования к безопасности
### 6.1. Защита данных:
- Валидация всех входных данных
- Подготовленные SQL запросы
- Шифрование конфиденциальных данных
- Резервное копирование

### 6.2. Обработка ошибок:
- Логирование всех исключений
- User-friendly сообщения об ошибках
- Восстановление после сбоев
- Проверка целостности данных

## 7. Требования к развёртыванию
### 7.1. Установка:
```
# Клонирование репозитория
git clone https://github.com/username/knowledge-journal.git

# Установка зависимостей
pip install -r requirements.txt

# Настройка базы данных
python scripts/setup_database.py

# Запуск приложения
python src/app/main.py
```

### 7.2. Конфигурация:
```
# config.yaml
database:
  host: localhost
  port: 5432
  name: knowledge_journal
  user: postgres
  password: secret_password

paths:
  notes_directory: ./data/notes_md
  exports_directory: ./data/exports
  templates_directory: ./data/templates

app:
  theme: dark
  language: ru
  auto_save: true
  auto_save_interval: 300  # секунд
```

---

**Разработал:**  
___________________________  
**Курносенко Александр Сергеевич**  
Студент группы 21ИС-24  
Дата: 29.01.2026

**Принял:**  
___________________________  
**Бобошко Михаил Николаевич**  
Руководитель учебной практики  
Дата: ___________________

---
